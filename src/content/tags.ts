import { TAGS } from "../lib/types";

/**
 * Exported canonical tag list used across the site. Edit this array
 * to add/remove domain tags. Keep entries capitalized for display.
 */
export const TAG_LIST = TAGS;

export default TAG_LIST;
